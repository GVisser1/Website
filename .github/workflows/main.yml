name: CI
on: pull_request
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      # TODO: Get a real domain
      #- name: Run E2E tests 🧪
      #  uses: cypress-io/github-action@v2

      - name: Run Component tests 🧪
        uses: cypress-io/github-action@v2
        with:
          install: false
          command: yarn cypress run-ct
